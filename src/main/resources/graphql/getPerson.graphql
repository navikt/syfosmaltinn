query($ident: ID!){
  personResponse: hentPerson(ident: $ident) {
  	navn(historikk: false) {
  	  fornavn
  	  mellomnavn
  	  etternavn
    }
  }
  aktorIdResponse: hentIdenter(ident: $ident, grupper: AKTORID, historikk: false) {
      aktorIder: identer {
        aktorId: ident
      }
    }
}